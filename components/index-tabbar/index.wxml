<view>
  <van-tabbar active="{{ active }}" bind:change="onChange">
    <van-tabbar-item
      wx:for="{{panels}}"
      wx:key="name"
      name="{{item.name}}"
      icon="{{item.icon}}"
      info="{{item.badge}}">
      {{item.label}}
    </van-tabbar-item>
  </van-tabbar>
</view>
